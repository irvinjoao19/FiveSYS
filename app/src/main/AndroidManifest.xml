<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fivesys.alphamanufacturas.fivesys">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Views.Activities.MainActivity">

        </activity>
        <activity android:name=".Views.Activities.LoginActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Views.Activities.AuditoriaActivity" />
        <activity android:name=".Views.Activities.PerfilActivity" />
        <activity android:name=".Views.Activities.ConfigurationActivity" />
    </application>

</manifest>